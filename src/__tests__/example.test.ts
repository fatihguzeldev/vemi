import { describe, it, expect } from 'vitest'

/**
 * example test file to verify vitest setup.
 * this will be replaced with actual compiler tests.
 */
describe('vemi test setup', () => {
  it('should run tests', () => {
    expect(true).toBe(true)
  })
})
